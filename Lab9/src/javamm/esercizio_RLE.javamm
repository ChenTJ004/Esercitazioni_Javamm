String stringaDecompressa(int[] S) {
	String D = "";
	for (int i = 0; i < S.length; i += 2) {
		for (int j = 0; j < S[i + 1]; j++) {
			D += S[i];
		}
	}
	return D;
}

int[] stringaCompressa = {0, 3, 1, 2, 0, 5, 1, 3, 0, 1};
System.out.println(stringaDecompressa(stringaCompressa));
